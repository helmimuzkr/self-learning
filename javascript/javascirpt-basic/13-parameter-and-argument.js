// parameter adalah variabel yang ditulis di dalam kurung pada saat fungsi dibuat.
// parameter digunakan untuk menampung nilai dari argument.
// biasanya parameter itu variabel untuk inputan

// struktur
function namaFunction(parameter) {
    return parameter;
}

// argument adalah variabel yang ditulis di dalam kurung pada saat pemanggilan fungsi
// argument digunakan untuk mengirim nilai ke Parameter.

// struktur
// namaFunction(argument);

// impelemntasi
function jumlah(a, b) {
    return a + b;
}

let hasil = jumlah(1, 2);

console.log(hasil);
