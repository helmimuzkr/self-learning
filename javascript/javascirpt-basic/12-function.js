// function adalah block program atau sub program yang bisa dieksekusi dengan cara dipanggil. bisa dilakukan berulang kali
// function == method.

// strukturnya
function namaFunction() {
    // body block
}

namaFunction(); // untuk memanggil function

/*  Function ini biasanya disebut function declaration.
    Function declaration wajib diberikan nama, supaya bisa dipanggil.
    Nama pada function direkomendasikan menggunakan camelCase. */
// contoh
function volCube() {
    let cube = 3 * 3 * 3;
    console.log(cube);
}

volCube();
